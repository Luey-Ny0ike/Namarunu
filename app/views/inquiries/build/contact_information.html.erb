<div class="container wizard-steps-layout">
  <h3>Contact Information</h3>
  <%= form_for @inquiry, data: { controller: "recaptcha", action: "submit->recaptcha#submit", recaptcha_function_value: "executeRecaptchaForInquirySubmitAsync" } do |form| %>
      <div class="row justify-content-center">
        <% if @inquiry.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@inquiry.errors.count, "error") %> prohibited this inquiry from being saved:</h2>

            <ul>
              <% @inquiry.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

      <div class="field col-md-8 ">
        <%= form.label :full_name %>
        <%= form.text_field :full_name, class:"form-control", required: true %>
      </div>

      <div class="field col-md-8 mt-1">
        <%= form.label :phone_number %>
        <%= form.text_field :phone_number, class:"form-control", required: true %>
      </div>

      <div class="field col-md-8 mt-1">
        <%= form.label :email, "Email (optional)" %>
        <%= form.text_field :email, class:"form-control" %>
      </div>
      <div class="field col-md-8 mt-2">
        <%= recaptcha_v3(action: "inquiry_submit", turbo: true) %>
      </div>
      <div class="field col-md-8">
        <br>
        <%= form.submit 'Next Step (1/3)', class:"btn btn-outline-success next-button" %>
      </div>
      </div>
  <% end %>

</div>
